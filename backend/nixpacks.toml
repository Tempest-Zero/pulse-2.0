# Nixpacks configuration for Railway deployment
# Uses virtual environment to avoid "externally-managed-environment" error

[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = [
    "python -m venv --copies /app/venv",
    "/app/venv/bin/pip install --upgrade pip",
    "/app/venv/bin/pip install -r requirements.txt"
]

# NOTE: Database initialization happens at runtime in main.py
# Do NOT add init_db() here - postgres.railway.internal is only available at runtime

[start]
cmd = "/app/venv/bin/uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"

[variables]
PYTHONUNBUFFERED = "1"
